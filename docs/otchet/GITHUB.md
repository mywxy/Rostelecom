# Публикация на GitHub

Репозиторий: код в корне `todo-dapp`, отчёт в `docs/otchet/`.

## Push (после создания репозитория на github.com)

```powershell
cd "c:\Users\Matthew\OneDrive\Desktop\project seti\todo-dapp"

git remote add origin https://github.com/ВАШ_ЛОГИН/ИМЯ-РЕПО.git
git push -u origin main
```

Замените URL. Если `remote origin` уже есть: `git remote set-url origin <URL>`.

## Ссылка в PDF

В отчёте §8 укажите: `https://github.com/ВАШ_ЛОГИН/ИМЯ-РЕПО`
